---
- name: balancer
  src: http://rhce.domain7.example.com/materials/haproxy.tar

- name: phpinfo
  src: http://rhce.domain7.example.com/materials/phpinfo.tar

# ansible-galaxy role install -r requirements.yml -p roles
# ansible-galaxy role list